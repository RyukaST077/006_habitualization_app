---
title: タスク詳細（T-003）
task_id: "T-003"
source: "docs/implements_plan.md"
priority: "P0"
tdd_step: "基盤"
related_ids:
  - "N/A（基盤タスク）"
created: "20260219"
updated: "20260219"
version: "1.0.0"
---

# T-003: Unit/ITテスト基盤導入

## 概要

- **目的**: Unit/ITを継続実行できるテスト基盤を整備し、後続タスク（テスト作成・CI導入）の前提を確立する。
- **対象**: Test
- **依存**: T-001
- **後続**: T-004, T-005, T-008, T-010, T-012, T-014, T-026
- **適用パターン**: パターンD（インフラ/基盤タスク）

## 参照設計書

| ドキュメント | 参照箇所 |
|-------------|----------|
| `docs/implements_plan.md` | Phase 1, `T-003` |
| `docs/01_Project_Design/03_Architecture.md` | 1.1 前提（全面TypeScript）, 2.1 クライアントサイド（Next.js/TypeScript）, 9. CI/CD（PR品質ゲート） |
| `docs/01_Project_Design/10_Security_Design.md` | 4.3 鍵・シークレット管理, 5.2 入力検証, 5.5 依存関係管理 |
| `docs/01_Project_Design/12_Test_Plan.md` | 2.1 テストレベル定義（UT/IT）, 5.2 ツール, 6.1 品質目標 |
| `docs/development/environment_validation.md` | 6. テスト実行時チェック, 11. テストデータ更新時の確認フロー |

---

## PR-001: Unitテスト実行基盤の構築 [x]

- **目的**: Unitテストをローカルで再現可能にするため、テストランナー・設定・実行スクリプトを整備する。
- **TDDステップ**: 基盤

### 前提条件（自動検証可能）
| 条件 | 検証方法 | 期待結果 |
|------|----------|----------|
| T-001 の基盤ファイルが存在すること | `test -f README.md && test -f .gitignore` | exit code 0 |
| テスト計画書が存在すること | `test -f docs/01_Project_Design/12_Test_Plan.md` | exit code 0 |

### 変更予定ファイル
| 種別 | パス | 備考 |
|------|------|------|
| 新規 | `package.json` | `test` スクリプトとテスト依存関係を定義 |
| 新規 | `tsconfig.json` | TypeScript共通設定 |
| 新規 | `tsconfig.test.json` | テスト向け型チェック設定 |
| 新規 | `vitest.config.ts` | Unit/IT向け実行・カバレッジ設定 |
| 新規 | `tests/unit/sample.test.ts` | Unitテスト雛形 |
| 変更 | `README.md` | テスト基盤セットアップ/実行手順を追記 |
| 生成物 | `coverage/v8/lcov.info` | テスト実行時のカバレッジ成果物 |

### 実行タスク
- [x] `package.json` に Unit/IT 実行スクリプト（`npm run test`）と必要依存を定義する。
- [x] `tsconfig*.json` と `vitest.config.ts` を作成し、TypeScriptベースのテスト実行を可能にする。
- [x] `tests/unit/sample.test.ts` を追加し、最小のUnit実行パスを確立する。

### 受け入れ基準
| 条件 | 検証方法 | 期待結果 | ステータス |
|------|----------|----------|-----------|
| テスト設定ファイルが存在する | `test -f package.json && test -f vitest.config.ts && test -f tsconfig.test.json` | exit code 0 | `pass` |
| Unitテスト雛形が存在する | `test -f tests/unit/sample.test.ts` | exit code 0 | `pass` |
| Unitテストが実行可能である | `npm run test -- tests/unit/sample.test.ts` | exit code 0 | `pass` |

### セキュリティチェック
- [x] ハードコードされたシークレットがないこと
- [x] ユーザー入力の検証が実装されていること（該当なし: 入力受付機能なし）
- [x] SQLインジェクション対策（パラメータ化クエリ）（該当なし: DBアクセス実装なし）
- [x] XSS対策（HTMLサニタイズ）（該当なし: UIレンダリング変更なし）
- [x] 認証/認可の確認（テスト設定で `service_role` を不要にし、公開キーのみ利用する）

---

## PR-002: Integrationテスト雛形と環境ガード整備 [ ]

- **目的**: IT用の環境検証と固定fixtureを導入し、接続先誤設定や不安定データによるテスト失敗を防ぐ。
- **TDDステップ**: 基盤

### 前提条件（自動検証可能）
| 条件 | 検証方法 | 期待結果 |
|------|----------|----------|
| PR-001 が完了していること | `test -f package.json && test -f vitest.config.ts` | exit code 0 |
| 環境変数雛形が存在すること | `test -f .env.example && test -f .env.stg.example && test -f .env.prod.example` | exit code 0 |

### 変更予定ファイル
| 種別 | パス | 備考 |
|------|------|------|
| 新規 | `tests/integration/supabase-connectivity.test.ts` | IT接続検証テストの雛形 |
| 新規 | `tests/helpers/fixtures.ts` | 固定fixture（`USER-A`/`USER-B`/`OPS-1`）定義 |
| 新規 | `tests/helpers/env-guard.ts` | 本番接続禁止・必須環境変数チェック |
| 変更 | `README.md` | IT実行手順と前提環境変数を追記 |
| 変更 | `docs/development/environment_validation.md` | Unit/IT実行チェック手順の整合を更新 |
| 生成物 | `N/A` | 自動生成物なし |

### 実行タスク
- [ ] `tests/helpers/env-guard.ts` で `SUPABASE_ENV`/`SUPABASE_URL` 必須チェックと本番禁止ガードを実装する。
- [ ] `tests/helpers/fixtures.ts` に固定fixtureを定義し、Unit/ITで共通利用可能にする。
- [ ] `tests/integration/supabase-connectivity.test.ts` を追加し、環境整合性の検証経路を用意する。

### 受け入れ基準
| 条件 | 検証方法 | 期待結果 | ステータス |
|------|----------|----------|-----------|
| ITテスト雛形が存在する | `test -f tests/integration/supabase-connectivity.test.ts` | exit code 0 | `pass` |
| fixtureが存在する | `test -f tests/helpers/fixtures.ts` | exit code 0 | `pass` |
| Integrationテストが実行可能である | `npm run test -- tests/integration/supabase-connectivity.test.ts` | exit code 0 | `pass` |
| 本番接続ガードが機能する | `SUPABASE_ENV=production npm run test -- tests/integration/supabase-connectivity.test.ts` | `Production forbidden` で失敗 | `pass` |

### セキュリティチェック
- [ ] ハードコードされたシークレットがないこと
- [ ] ユーザー入力の検証が実装されていること（環境変数値の形式/許可値を検証する）
- [ ] SQLインジェクション対策（パラメータ化クエリ）（該当なし: 接続確認のみ、文字列連結SQLを追加しない）
- [ ] XSS対策（HTMLサニタイズ）（該当なし: UI変更なし）
- [ ] 認証/認可の確認（本番環境・特権キー利用をテスト実行時に禁止する）

---

## PR-003: カバレッジ出力と運用検証手順の固定化 [ ]

- **目的**: テスト結果の証跡を標準化し、後続のCI/CDタスクでそのまま利用できる状態にする。
- **TDDステップ**: 基盤

### 前提条件（自動検証可能）
| 条件 | 検証方法 | 期待結果 |
|------|----------|----------|
| PR-002 が完了していること | `test -f tests/integration/supabase-connectivity.test.ts && test -f tests/helpers/env-guard.ts` | exit code 0 |
| 環境検証ドキュメントが存在すること | `test -f docs/development/environment_validation.md` | exit code 0 |

### 変更予定ファイル
| 種別 | パス | 備考 |
|------|------|------|
| 変更 | `vitest.config.ts` | `coverage/v8/lcov.info` 出力設定を確定 |
| 変更 | `package.json` | カバレッジ付きテスト実行スクリプトを整備 |
| 変更 | `docs/development/environment_validation.md` | テスト実行時チェック（成果物確認）を更新 |
| 変更 | `README.md` | カバレッジ確認手順と失敗時の切り分けを追記 |
| 生成物 | `coverage/v8/lcov.info` | カバレッジ証跡 |

### 実行タスク
- [ ] テスト実行時に `coverage/v8/lcov.info` を必ず生成する設定にする。
- [ ] ローカル実行コマンドと確認手順を `README.md` / `docs/development/environment_validation.md` に反映する。
- [ ] Unit/ITの最小実行セットでカバレッジ成果物生成を確認する。

### 受け入れ基準
| 条件 | 検証方法 | 期待結果 | ステータス |
|------|----------|----------|-----------|
| カバレッジ設定が存在する | `rg \"coverage|v8|lcov\" vitest.config.ts package.json` | 1件以上ヒット | `pass` |
| テスト実行後に成果物が生成される | `npm run test && test -f coverage/v8/lcov.info` | exit code 0 | `pass` |
| ドキュメントに検証手順が反映される | `rg \"coverage/v8/lcov.info|npm run test\" README.md docs/development/environment_validation.md` | 1件以上ヒット | `pass` |

### セキュリティチェック
- [ ] ハードコードされたシークレットがないこと
- [ ] ユーザー入力の検証が実装されていること（該当なし: 入力受付機能なし）
- [ ] SQLインジェクション対策（パラメータ化クエリ）（該当なし: DBクエリ変更なし）
- [ ] XSS対策（HTMLサニタイズ）（該当なし: UI変更なし）
- [ ] 認証/認可の確認（テストログ/カバレッジ成果物に機密値を出力しない）

---

## 未確定事項（TBD）

| ID | 項目 | 影響範囲 | 確認先 |
|----|------|----------|--------|
| TBD-000 | なし（2026-02-19時点で未確定事項なし） | N/A | N/A |

---

## コミット計画

| 順序 | コミットメッセージ | 内容 |
|------|-------------------|------|
| 1 | `[T-003] タスクファイル作成` | `_tasks/T-003.md` 作成 |
| 2 | `[T-003][PR-001] Unitテスト実行基盤の構築` | PR-001 の実装 |
| 3 | `[T-003][PR-002] Integrationテスト雛形と環境ガード整備` | PR-002 の実装 |
| 4 | `[T-003][PR-003] カバレッジ出力と運用検証手順の固定化` | PR-003 の実装 |

---

更新日: 20260219
バージョン: 1.0.0
