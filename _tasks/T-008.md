---
title: タスク詳細（T-008）
task_id: "T-008"
source: "docs/implements_plan.md"
priority: "P1"
tdd_step: "基盤"
related_ids:
  - "FNC-013"
  - "IF-005"
  - "ROLE-002"
  - "AC-030"
created: "20260212"
updated: "20260212"
version: "1.0.0"
---

# T-008: テストフィクスチャ戦略（USER-A/B/ROLE-002）整備

## 概要

- **目的**: テストデータを再現可能な固定フィクスチャとして標準化し、UT/IT/E2Eで `USER-A` / `USER-B` / `OPS-1(ROLE-002)` を一貫して利用できる基盤を確立する。
- **対象**: Test Data（`USER-A/B/ROLE-002` フィクスチャ戦略）
- **依存**: `T-003`
- **後続**: なし（`docs/implements_plan.md` に明示なし）
- **適用パターン**: D（インフラ/基盤タスク）

## 参照設計書

| ドキュメント | 参照箇所 |
|-------------|----------|
| `docs/implements_plan.md` | Phase 1 `T-008` 行（P1、依存 `T-003`、TDDステップ `基盤`） |
| `docs/01_Project_Design/12_Test_Plan.md` | 5.3 テストデータ方針（A/B/ROLE-002固定シード）、6.1 セキュリティ基準（RLS違反0） |
| `docs/01_Project_Design/13_Test_Specs/00_Test_Common.md` | 1.3 テストデータ準備方針（`USER-A`,`USER-B`,`OPS-1`）、ケース単位で再現可能 |
| `docs/01_Project_Design/05_Feature_List.md` | 7.3 ロール対応表（`ROLE-002` は運用者、CLI/SQL中心） |
| `docs/01_Project_Design/10_Security_Design.md` | 最小権限、RLS前提、監査可能性、重大/高脆弱性0 |
| `docs/02_Detailed_Design/11_Module_Design/M-014_AuthorizationPolicyService.md` | ロール型定義（`ROLE-001` / `ROLE-002` / `system`） |

---

## PR-001: フィクスチャ戦略を定義 [x]

- **目的**: `USER-A/B/OPS-1` の用途・命名規約・再現ルールを定義し、後続実装の共通契約を固定する。
- **TDDステップ**: 基盤

### 前提条件（自動検証可能）
| 条件 | 検証方法 | 期待結果 |
|------|----------|----------|
| `T-003` のタスクファイルが存在すること | `test -f _tasks/archive/T-003.md` | exit code 0 |
| テスト計画書が存在すること | `test -f docs/01_Project_Design/12_Test_Plan.md` | exit code 0 |
| テスト共通仕様が存在すること | `test -f docs/01_Project_Design/13_Test_Specs/00_Test_Common.md` | exit code 0 |

### 変更予定ファイル
| 種別 | パス | 備考 |
|------|------|------|
| 変更 | `tests/helpers/fixtures.ts` | フィクスチャ契約（ロール/属性/命名）を明文化 |
| 変更 | `tests/e2e/fixtures/users.ts` | E2Eのユーザーキーと運用ルールを揃える |
| 変更 | `README.md` | テストデータ戦略の概要と実行入口を追記 |

### 実行タスク
- [x] `USER-A` / `USER-B` / `OPS-1` の責務（正常系・他者参照拒否・運用検証）をコード上の型とコメントに反映する。
- [x] フィクスチャ命名規則（`USER_A`,`USER_B`,`OPS_1`）とロール対応（`ROLE-001/ROLE-002`）を統一する。
- [x] テストデータはダミーのみ使用する方針をREADMEに追記する。

### 受け入れ基準
- [x] `tests/helpers/fixtures.ts` に `USER_A` / `USER_B` / `OPS_1` が定義されていること（`rg "USER_A|USER_B|OPS_1" tests/helpers/fixtures.ts`）。
- [x] `tests/helpers/fixtures.ts` にロール区分が明示されていること（`rg "role|ROLE-001|ROLE-002|USER|OPS" tests/helpers/fixtures.ts`）。
- [x] `README.md` にテストデータ方針（固定フィクスチャ、ダミーデータのみ）が記載されていること（`rg "テストデータ|fixture|ダミー" README.md`）。

### セキュリティチェック
- [x] ハードコードされたシークレットがないこと
- [x] ユーザー入力の検証が実装されていること（該当する場合）
- [x] SQLインジェクション対策（パラメータ化クエリ）を壊す変更がないこと
- [x] XSS対策（HTMLサニタイズ）を壊す変更がないこと
- [x] 認証/認可の確認（ロール境界/RLS前提を崩していないこと）

---

## PR-002: UT/IT向け共通フィクスチャを実装 [ ]

- **目的**: UT/ITで再利用可能な固定ユーザー生成ヘルパーを整備し、テストの再現性を高める。
- **TDDステップ**: 基盤

### 前提条件（自動検証可能）
| 条件 | 検証方法 | 期待結果 |
|------|----------|----------|
| PR-001 が完了していること | `rg -n "^## PR-001:.*\[x\]" _tasks/T-008.md` | 1件ヒット |
| テストヘルパーの配置先が存在すること | `test -d tests/helpers` | exit code 0 |
| Unit/Integration テストの実行基盤が存在すること | `test -f vitest.config.ts` | exit code 0 |

### 変更予定ファイル
| 種別 | パス | 備考 |
|------|------|------|
| 変更 | `tests/helpers/fixtures.ts` | フィクスチャ生成の公開APIを整備 |
| 変更 | `tests/integration/setup.ts` | IT実行前のフィクスチャ初期化を統一 |
| 変更 | `tests/unit/sample.test.ts` | フィクスチャ利用例を反映（必要時） |

### 実行タスク
- [ ] テストごとに独立したユーザーオブジェクトを返すヘルパーを実装する。
- [ ] `USER-A` / `USER-B` / `OPS-1` の属性（id/role/timezone/locale）を固定値で定義する。
- [ ] ITの初期化処理で共通ヘルパーを参照する導線を整備する。

### 受け入れ基準
- [ ] フィクスチャ生成関数が毎回新しいオブジェクトを返すことを確認できるテストがあること（`npm run test -- tests/unit/sample.test.ts` が成功）。
- [ ] `tests/integration/setup.ts` から共通フィクスチャが参照されていること（`rg "fixtures|USER_A|OPS_1" tests/integration/setup.ts`）。
- [ ] `npm run test` が成功すること（exit code 0）。

### セキュリティチェック
- [ ] ハードコードされたシークレットがないこと
- [ ] ユーザー入力の検証が実装されていること（該当する場合）
- [ ] SQLインジェクション対策（パラメータ化クエリ）を壊す変更がないこと
- [ ] XSS対策（HTMLサニタイズ）を壊す変更がないこと
- [ ] 認証/認可の確認（ロール境界/RLS前提を崩していないこと）

---

## PR-003: E2E固定ユーザー運用を整備 [ ]

- **目的**: E2Eで `USER-A/B/OPS-1` を環境変数経由で安全に利用できるようにし、Dev/Stgの実行方針を明確化する。
- **TDDステップ**: 基盤

### 前提条件（自動検証可能）
| 条件 | 検証方法 | 期待結果 |
|------|----------|----------|
| PR-002 が完了していること | `rg -n "^## PR-002:.*\[x\]" _tasks/T-008.md` | 1件ヒット |
| E2Eフィクスチャの配置先が存在すること | `test -d tests/e2e/fixtures` | exit code 0 |
| E2E環境ガードが存在すること | `test -f tests/e2e/setup/env-guard.ts` | exit code 0 |

### 変更予定ファイル
| 種別 | パス | 備考 |
|------|------|------|
| 変更 | `tests/e2e/fixtures/users.ts` | 固定ユーザーキーと環境変数マッピングを整理 |
| 変更 | `tests/e2e/setup/env-guard.ts` | Dev/Stgの実行ポリシー検証を追加 |
| 変更 | `.env.example` | E2E用ダミー環境変数キーを明記 |

### 実行タスク
- [ ] `E2E_USER_A_EMAIL` / `E2E_USER_B_EMAIL` / `E2E_OPS_1_EMAIL` の利用ルールを統一する。
- [ ] Stgでは実接続、Devではモック許容の方針を実行時に判定できるようにする。
- [ ] シークレット値をコードへ直書きしない方針をE2Eフィクスチャで徹底する。

### 受け入れ基準
- [ ] `tests/e2e/fixtures/users.ts` に `USER_A` / `USER_B` / `OPS_1` の環境変数参照があること（`rg "E2E_USER_A_EMAIL|E2E_USER_B_EMAIL|E2E_OPS_1_EMAIL" tests/e2e/fixtures/users.ts`）。
- [ ] `tests/e2e/setup/env-guard.ts` で実行環境ポリシーが検証されること（`rg "SUPABASE_ENV|stg|dev" tests/e2e/setup/env-guard.ts`）。
- [ ] `npm run test:e2e:smoke` が実行可能であること（必要環境未設定時はガードで明示的に失敗する）。

### セキュリティチェック
- [ ] ハードコードされたシークレットがないこと
- [ ] ユーザー入力の検証が実装されていること（該当する場合）
- [ ] SQLインジェクション対策（パラメータ化クエリ）を壊す変更がないこと
- [ ] XSS対策（HTMLサニタイズ）を壊す変更がないこと
- [ ] 認証/認可の確認（ロール境界/RLS前提を崩していないこと）

---

## PR-004: テストデータ運用ドキュメントを確定 [ ]

- **目的**: フィクスチャ更新手順・検証手順・セキュリティ注意点を文書化し、運用を再現可能にする。
- **TDDステップ**: 基盤

### 前提条件（自動検証可能）
| 条件 | 検証方法 | 期待結果 |
|------|----------|----------|
| PR-003 が完了していること | `rg -n "^## PR-003:.*\[x\]" _tasks/T-008.md` | 1件ヒット |
| セキュリティ運用文書が存在すること | `test -f docs/development/secrets_management.md` | exit code 0 |
| 環境検証文書が存在すること | `test -f docs/development/environment_validation.md` | exit code 0 |

### 変更予定ファイル
| 種別 | パス | 備考 |
|------|------|------|
| 変更 | `docs/development/environment_validation.md` | フィクスチャ検証チェックを追記 |
| 変更 | `docs/development/secrets_management.md` | E2Eユーザー情報の取扱いを追記 |
| 変更 | `README.md` | テストデータ更新と確認コマンドを追記 |

### 実行タスク
- [ ] フィクスチャ更新時の確認フロー（unit/integration/e2e）を定義する。
- [ ] ROLE-002関連の取り扱い（匿名KPI/監査用途）を明記する。
- [ ] 失敗時の切り分け手順（env不足、フィクスチャ不整合、ロール不一致）を記述する。

### 受け入れ基準
- [ ] `docs/development/environment_validation.md` にテストデータ検証手順があること（`rg "fixture|USER-A|USER-B|OPS-1|ROLE-002" docs/development/environment_validation.md`）。
- [ ] `docs/development/secrets_management.md` にE2Eユーザー情報の管理方針があること（`rg "E2E|テストユーザー|シークレット" docs/development/secrets_management.md`）。
- [ ] `README.md` にテスト実行コマンド（`npm run test`, `npm run test:e2e:smoke`）が記載されていること。

### セキュリティチェック
- [ ] ハードコードされたシークレットがないこと
- [ ] ユーザー入力の検証が実装されていること（該当する場合）
- [ ] SQLインジェクション対策（パラメータ化クエリ）を壊す変更がないこと
- [ ] XSS対策（HTMLサニタイズ）を壊す変更がないこと
- [ ] 認証/認可の確認（ロール境界/RLS前提を崩していないこと）

---

## 未確定事項（TBD）

| ID | 項目 | 影響範囲 | 確認先 |
|----|------|----------|--------|
| - | 未確定事項なし（20260212時点で計画化に必要な情報は設計書で確認済み） | - | - |

---

## コミット計画

| 順序 | コミットメッセージ | 内容 |
|------|-------------------|------|
| 1 | `[T-008] タスクファイル作成` | `_tasks/T-008.md` 作成 |
| 2 | `[T-008][PR-001] フィクスチャ戦略を定義` | PR-001 の実装 |
| 3 | `[T-008][PR-002] UT/IT向け共通フィクスチャを実装` | PR-002 の実装 |
| 4 | `[T-008][PR-003] E2E固定ユーザー運用を整備` | PR-003 の実装 |
| 5 | `[T-008][PR-004] テストデータ運用ドキュメントを確定` | PR-004 の実装 |

---

更新日: 20260212
バージョン: 1.0.0
