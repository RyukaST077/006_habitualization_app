# 概要

- このPRで何を変更したかを簡潔に記載してください。

# タスク紐付け

- タスクID（必須）: `T-xxx`
- 関連Issue（任意）: `#123`

# IDトレーサビリティ（必須）

## 対象ID（FNC/SCR/IF/TBL）

- FNC: `FNC-xxx`
- SCR: `SCR-xxx`
- IF: `IF-xxx`
- TBL: `TBL-xxx`
- 該当なし: `N/A（理由）`

## 要件ID（FR/NFR/CON/AC）

- 要件ID: `FR-xxx, NFR-xxx, CON-xxx, AC-xxx`
- 該当なし: `N/A（理由）`

## テストID（TC/TS）

- テストID: `TC-xxx, TS-xxx`
- 該当なし: `N/A（理由）`

# 品質確認（必須）

- [ ] `npm run lint` が成功している
- [ ] `npm run typecheck` が成功している
- [ ] `npm run test` が成功している

# セキュリティ確認（必須）

- [ ] 入力検証: 追加/変更した入力項目のバリデーションを確認した
- [ ] 認証: 既存の認証フローを壊していない
- [ ] 認可: 権限境界（RLS含む）を壊していない
- [ ] シークレット: 平文シークレットを追加していない

# テスト証跡リンク（必須）

- CI実行結果:
- テストログ:
- 画面キャプチャ/動画:
- 該当なし: `N/A（理由）`

# レビューポイント（任意）

- 重点的に見てほしい点があれば記載してください。
